name: Build and Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build
        run: echo "Building..."

      - name: Save build status
        id: save_build_status
        if: always()  # Always save build status
        run: echo "var2=${{ job.status }}" >> $GITHUB_ENV

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Test
        run: |
          echo "Testing..."
          exit 1  # Simulating a failure for demonstration

      - name: Save test status
        id: save_test_status
        if: always()  # Always save test status
        run: echo "var1=${{ job.status }}" >> $GITHUB_ENV

  overall_status:
    name: Determine Overall Status
    runs-on: ubuntu-latest
    needs: [build, test]
    if: always()  # Always run this job

    steps:
      - name: Set Overall Status
        id: set_overall_status
        run: |
          build_status="${{ env.var2 }}"
          test_status="${{ env.var1 }}"
          echo "Build Status: $build_status"
          echo "Test Status: $test_status"
          
          if [ "$build_status" == "success" ] && [ "$test_status" == "success" ]; then
            echo "Overall Status: pass"
            echo "status=pass" >> $GITHUB_ENV
          else
            echo "Overall Status: fail"
            echo "status=fail" >> $GITHUB_ENV
          fi

      - name: Save Overall Status
        run: echo "status=${{ env.status }}" >> $GITHUB_ENV

  notify:
    name: Notify
    runs-on: ubuntu-latest
    needs: overall_status  # Wait for overall_status job to complete

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Send Notifications
        uses: ./.github/actions/build_and_notify
        with:
          action-name: Build and Test Notification
          overall-status: ${{ env.status }}
