name: Build and Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Build
        run: echo "Building..."
      
      - name: Save build status
        id: save_build_status
        if: always()
        run: echo "var2=${{ job.status }}" >> $GITHUB_ENV

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Test
        run: |
          echo "Testing..."
          # exit 1
      
      - name: Save test status
        id: save_test_status
        if: always()
        run: echo "var1=${{ job.status }}" >> $GITHUB_ENV

  overall_status:
    name: Determine Overall Status
    runs-on: ubuntu-latest
    needs: [build, test]
    if: always()
    outputs:
      overall-status: ${{ needs.overall_status.outputs.overall-status }} # Pass overall status as an output

    steps:
      - name: Set Overall Status
        id: set_overall_status
        run: |
          build_status=$(echo "${{ needs.build.outputs.var2 }}")
          test_status=$(echo "${{ needs.test.outputs.var1 }}")
          # Check if both build and test were successful
          if [ "$build_status" == "success" ] && [ "$test_status" == "success" ]; then
            echo "Overall Status: pass"
            echo "overall_status=pass" >> $GITHUB_ENV # Set overall status as an environment variable
          else
            echo "Overall Status: fail"
            echo "overall_status=fail" >> $GITHUB_ENV # Set overall status as an environment variable
          fi

  save_overall_status:
    name: Save Overall Status
    if: always()
    needs: [overall_status]
    runs-on: ubuntu-latest
    steps:
      - name: Save Overall Status
        run: |
          echo "Overall Status: ${{ env.overall_status }}" # Print overall status to verify
